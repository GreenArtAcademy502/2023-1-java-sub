package com.green.javasub.ch02;

public class VariableType {
    public static void main(String[] args) {
        // 기본타입, 원시타입 (Primitive Type)
        // 타입이름이 전부 소문자
        // 정수형(4) > byte, short, int, long
        // 실수형(2) > float, double
        // 불린형(1) > boolean
        // 문자형(1) > char

        // 참조타입 (Reference Type)
        // 타입이름이 파스칼 케이스 기법의 이름(첫문자 대문자로 시작)
        // 참조타입은 클래스이기 때문
        // 참조타입은 객체의 주소값이 저장된다.
        // 기본타입 제외한 모든 타입

        VariableType a;
    }
}
